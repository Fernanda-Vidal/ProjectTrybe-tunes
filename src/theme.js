import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#529186',
      light: '#8DC2BB',
    },
    secondary: {
      main: '#e1c686',
    },
    otherColor: {
      main: '#f1dacb',
    },
  },
});

export default theme;
